/** @typedef  {import("prettier").Config} PrettierConfig*/

/** @type { PrettierConfig } */
const config = {
  tabWidth: 2,
  semi: false,
  trailingComma: 'all',
  endOfLine: 'auto',
  singleQuote: true,
  useTabs: false,
  printWidth: 120,
}

module.exports = config
